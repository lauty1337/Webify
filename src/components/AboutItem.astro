---

const {time, title, description, delay = 0} = Astro.props; 

---

<div data-reveal style={`transition-delay: ${delay * 0.1}s`} class="relative pl-8 group">
  <!-- Punto centrado en la línea -->
  <span class="absolute left-1 top-0 size-4 -translate-x-1 rounded-full border border-gray-300 bg-white group-hover:border-blue-500 transition-all duration-200"></span>
  
  <!-- Card -->
  <article class="bg-gray-200/10 border border-black/7 rounded-2xl p-6 ">
    <span class="text-xs text-gray-400 mb-2 font-medium block group-hover:text-blue-400">
      {time}
    </span>
    
    <header>
      <h3 class="text-xl font-semibold mb-2 text-gray-900">
        {title}
      </h3>
    </header>
    
    <p class="text-gray-600 text-sm leading-relaxed">
      {description}
    </p>
  </article>
</div>